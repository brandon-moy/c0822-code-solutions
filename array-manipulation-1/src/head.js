/* exported head */

// Goal : return the first element of an array
// put array into function
// take value of array at position 0 (first value)
// return value of array at first position

function head(array) {
  return array[0];
}
