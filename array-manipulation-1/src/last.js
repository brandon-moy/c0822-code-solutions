/* exported last */

// Goal : return last element of an array
// put array into function
// create an index for the length of the array - 1
// return the value of the array at the index

function last(array) {
  var lastIndex = array.length - 1;
  return array[lastIndex];
}
