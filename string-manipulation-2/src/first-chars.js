/* exported firstChars */

// Goal: take a string and cut if off at the length provided
// take length and string
// start string from position 0 and end at length
// return new string

function firstChars(length, string) {
  return string.slice(0, length);
}
