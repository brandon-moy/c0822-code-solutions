/* exported setValue */

// Goal: no return but should add key value pair to object
// 3 arguments, object, key, value
// add key and value to object
// no return

function setValue(object, key, value) {
  object[key] = value;
}
