/* exported getValue */

// Goal: get value of object at key property location
// needs two arguments of value and key
// search for value at key location of object
// return the value

function getValue(object, key) {
  return object[key];
}
