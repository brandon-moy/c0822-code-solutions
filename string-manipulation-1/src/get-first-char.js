/* exported getFirstChar */

// Goal: get the first character of a string
// How?
// put a stirng into the function
// get the function to pull the string at position 0
// return the string at position 0

function getFirstChar(string) {
  return string[0];
}
